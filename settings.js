// Use if you want more functions than just settings.json

var settings = {
    // force macOs version of roblox (beta test) def. false
    "force-macOs-version": false,
    // should be enabled if you want use build-in executor
    "EnableRbxMe": true,
    // if enabled then window will pop-up with executor
    "UseBuildInExecutor": false,
    // same as luau-force-version, setting version from 2022 (MAY CAUSE ERRORS IN SCRIPTS)
    "Use2022luau": false,
    // forcing luau version def. null (null = not forcing verison)
    "luau-force-version": null
}
